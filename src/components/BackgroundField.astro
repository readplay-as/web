---
/**
 * Positional-tracking background: drifting nodes, links between near
 * neighbours, and the occasional pass travelling along one. Rendered in-page
 * rather than shipped as video — see src/scripts/field.js.
 */
---

<div class="bg">
  <canvas class="bg-field" id="bgField" aria-hidden="true"></canvas>
</div>
